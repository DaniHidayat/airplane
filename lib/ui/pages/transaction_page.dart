import 'package:flutter/material.dart';

class TrasactionPage extends StatelessWidget {
  const TrasactionPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child:Text('Trascation'),);
  }
} 